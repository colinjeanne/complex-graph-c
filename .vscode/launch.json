{
  "configurations": [
    {
      "name": "(lldb) Launch",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/complex-graph",
      "args": [],
      "cwd": "${fileDirname}",
      "disableASLR": false,
      "initCommands": [
        "settings set target.disable-aslr false"
      ],
      "preLaunchTask": "Debug Build",
    }
  ],
  "version": "2.0.0"
}